{#
	Guarded login header include.
	Ensures login header is rendered once per request.
-#}

{%
	if (!ctx.template_header_sent) {
		include("themes/" + theme + "/header_login");
		ctx.template_header_sent = true;
	}
%}
